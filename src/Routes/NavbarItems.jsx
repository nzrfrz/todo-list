import { 
    ThemeToggleButton,
} from "../Components";

export const NavbarItems = () => {
    return (
        <div className="navbar-item-container">
            <ThemeToggleButton />
        </div>
    );
};